---
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/showcase?useSSL=false
    username: root
    password: Octodb123
    testWhileIdle: true
    validationQuery: SELECT 1
  jpa:
    show-sql: true   
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
      dialect: org.hibernate.dialect.MySQL5Dialect
